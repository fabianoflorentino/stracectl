# v1.0.87 — Release Notes (updated)

Summary

- Primarily documentation and website updates.

Key Changes

- docs: adds `CHANGELOG.md` to the repository (mirror of the website changelog).
- website: updates homepage layout and content structure.
- website: adds HTTP Sidecar API feature and documentation.
- website(docs): standardizes WebSocket path to `/stream`.
- website(docs): standardizes API snapshot endpoint to `/api/stats` (previously `/api/syscalls`).
- website: updates footer to dynamically display license information.
- tooling: markdownlint — adds `LICENSE` to the ignore list.

Notes

- These changes are primarily documentation changes; there were no significant functional changes to the binary.

If you wish, I can also update `docs/CHANGELOG.md` with a local summary and open a PR.
