# v1.0.93 — Release Notes

Release date: 2026-03-17

Summary

- Improvements to CI/CD workflows and repository documentation, plus a new helper to manage changelog and release notes.

Highlights

- Tooling: add `make update-changelog` target and a helper script to manage release notes and changelog updates.
- CI/CD: add secret management for workflows and Docker configurations, plus checks for Docker Hub secrets and dry-run options for jobs.
- CI/CD: remove `GITHUB_TOKEN` from required secrets and revert recent orchestrator changes to restore previous workflow files.
- CI/CD: update workflow paths to include all subdirectories and fix indentation for cleanup comments.
- Docs / Website: add `CHANGELOG.md` (mirror of the site changelog), add v1.0.87 release notes, standardize the API snapshot endpoint to `/api/stats` (previously `/api/syscalls`) and the WebSocket path to `/stream`.
- Website: update footer to dynamically display license information.

Bug fixes

- Fix: remove `GITHUB_TOKEN` from required CI/CD secrets.
- Fix: correct indentation for optional cleanup comment in CI/CD workflow files.

Internal / Maintenance

- Chore: sync site and repository documentation and add a changelog mirror.
- CI: finalize secret management, add Docker Hub secret checks, and implement dry-run options for workflow jobs.

Notes

- No breaking changes to the binary or runtime behavior; this release focuses on infrastructure, CI/CD, and documentation improvements.
- To update the repository changelog and attach these notes to the GitHub Release, run:
  `make update-changelog VERSION=v1.0.93 NOTES_FILE=.changes/v1.0.93_release_notes.md`

Downloads

- Binaries and images are attached to the GitHub Release.

--
This file was generated from recent commits; please review before publishing.
